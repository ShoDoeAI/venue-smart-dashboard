{
  "buildCommand": "pnpm install --no-frozen-lockfile && cd packages/frontend && pnpm run build",
  "outputDirectory": "packages/frontend/dist",
  "installCommand": "npm install -g pnpm && pnpm install --no-frozen-lockfile",
  "framework": null
}
