{
  "buildCommand": "pnpm install --frozen-lockfile=false && pnpm build:frontend",
  "outputDirectory": "packages/frontend/dist",
  "installCommand": "corepack enable && corepack prepare pnpm@latest --activate && pnpm install --frozen-lockfile=false",
  "framework": null,
  "regions": ["sfo1"],
  "functions": {
    "api/**/*.js": {
      "runtime": "nodejs18.x"
    }
  }
}
